"""Legacy NiceGUI page package, now retired.  The NiceGUI front-end and
its per-page route handlers were removed when the UI was ported to Flask
+ Jinja templates; only home_stats.py remains here -- a pure data/logic
module still imported by the Flask app (app.py)."""
