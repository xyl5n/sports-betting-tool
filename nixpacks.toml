# Python version is pinned via runtime.txt (python-3.11.9) at the repo root.
# railway.toml sets builder = 'nixpacks' which reads runtime.txt automatically.
# This file is intentionally minimal to avoid overriding nixpacks auto-detection.
