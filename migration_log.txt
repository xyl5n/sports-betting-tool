Monolith decomposition: app/app.py → app/state.py + app/utils.py
=================================================================

Branch: claude/split-utils-state
Started: 2026-05-28
Source: app/app.py @ 13,004 lines on origin/main (post PR #275)

Rules followed for every move:
  - No logic changes; lines are lifted as-is, with a `# moved from app.py:NNN`
    marker above each block.
  - `from <module> import *` style; app/__init__.py NOT introduced.
  - state.py has an explicit __all__ list so underscore-prefixed names
    survive `from state import *`.

Names INTENTIONALLY left in app.py (would have broken if moved):
  - _logger        — used 100+ times inside app.py; moving and re-importing
                     adds noise with no benefit (user decision).
  - _werkzeug_main — runtime guard, not state (user decision).
  - _sched         — initial declaration line 1358, BUT reassigned at boot
                     line 12290 (`_sched = nightly_retrain.start()`).  Under
                     `from state import *`, the reassignment would only
                     rebind app.py's local name and state.py's _sched would
                     still be None.  Kept in app.py.
  - _STAT_LOOKUP_LOG_BUDGET — reassigned via `global` inside
                     _reset_stat_lookup_log_budget() (line 10776) and
                     _stat_lookup_log() (line 10854).  Same rebind problem
                     as _sched.  Kept in app.py.
  - _delete_file   — pure helper, but depends on _logger.  Would have moved
                     to utils.py; rerouted to "stay in app.py" because the
                     alternative was a circular import or a rewrite.
  - _audit_log     — same situation: depends on _eprint, which depends on
                     _redact + sys.stderr.  Kept in app.py to avoid pulling
                     _eprint along.

============================================================================
COMMIT 1 — app/state.py (43 items, post PR #275 line numbers)
============================================================================

Env constants (5)
  _LOG_LEVEL                       moved from app.py:138
  _ODDS_API_KEY                    moved from app.py:153
  _API_SPORTS_KEY                  moved from app.py:154
  _ANTHROPIC_API_KEY               moved from app.py:155
  _SEASON                          moved from app.py:156

Cache singleton + file path constants (12)
  _cache                           moved from app.py:305
  _ANALYSIS_CACHE_FILE             moved from app.py:307
  _WNBA_ANALYSIS_CACHE_FILE        moved from app.py:308
  _PRE_GAME_ODDS_FILE              moved from app.py:309
  _EXPLAIN_CACHE_FILE              moved from app.py:310
  _AI_BREAKDOWN_CACHE_FILE         moved from app.py:311
  _ANALYSIS_TIMESTAMPS_FILE        moved from app.py:315
  _AUTO_ANALYSIS_LOG_FILE          moved from app.py:1243
  _MODEL_SETTINGS_FILE             moved from app.py:1249
  _PICKS_HISTORY_FILES             moved from app.py:5903
  _ENSEMBLE_PICKS_FILE             moved from app.py:5909
  _BET_HISTORY_ARCHIVE             moved from app.py:5911

Snapshot + Supabase cache keys (4)
  _snapshot_lock                   moved from app.py:321
  _SNAPSHOT_ENABLED                moved from app.py:324
  _CACHE_KEY_SNAPSHOT              moved from app.py:340
  _CACHE_KEY_ANALYSIS_MLB          moved from app.py:341
  _CACHE_KEY_ANALYSIS_WNBA         moved from app.py:342

Analysis state dicts + locks (5)
  EV_MIN_EDGE                      moved from app.py:1213
  _analysis_state                  moved from app.py:1215
  _wnba_analysis_state             moved from app.py:1224
  _auto_analysis_lock              moved from app.py:1234
  _auto_analysis_state             moved from app.py:1235

Model-settings defaults + auto-settlement locks (5)
  _MODEL_SETTINGS_DEFAULT          moved from app.py:1250
  _auto_settlement_lock            moved from app.py:1326
  _refresh_cycle_lock              moved from app.py:1338
  _FEATURE_LABELS                  moved from app.py:1360
  _ODDS_FIELDS                     moved from app.py:1409

API base URLs + TTL constants (5)
  _MLB_STATS_BASE                  moved from app.py:2557
  _LINESCORE_TTL                   moved from app.py:2560
  _ESPN_WNBA_BASE                  moved from app.py:2637
  _QUARTER_ORDINAL                 moved from app.py:2640
  _DEBUG_LOG                       moved from app.py:3662

Analysis progress + model picks (4)
  _analysis_progress_lock          moved from app.py:4068
  _MLB_TEAM_NORM                   moved from app.py:10435
  _MODEL_PICK_STAT                 moved from app.py:10751
  _SETTLE_GAMELOG_TTL              moved from app.py:10765

Settlement bridge + AI throttle (2)
  _STATSAPI_BRIDGE_TTL             moved from app.py:10860
  _AI_RUN_DELAY                    moved from app.py:12125

Auxiliary aliases added to state.py:
  _threading             — alias of `threading`, matches app.py:282 alias
  _threading_for_analyze — alias of `threading`, matches app.py:3853 alias

app.py changes:
  - 231 lines removed (the verified ranges above, including any preceding
    comment block immediately above each item).
  - One `from state import *  # noqa: F401,F403` line added after
    `from pathlib import Path` near the top.
  - Net: 13004 lines → 12778 lines.

Verification (Phase 1):
  ✓ python3 -m py_compile state.py
  ✓ python3 -m py_compile app.py
  ✓ python3 -c "from state import *" with __all__ exposes 43 names
  ✓ ast.parse on app.py confirms no module-level redefinitions of moved names
  ✓ Moved names still referenced 333× in app.py (via star import)

============================================================================
COMMIT 2 — app/utils.py (26 items)
============================================================================

Date/time helpers (3)
  _today_et                        moved from app.py:[Phase 1 line 785]
  _game_et_date                    moved from app.py:[Phase 1 line 795]
  _et_date_of                      moved from app.py:[Phase 1 line 2806]

Odds + probability math (4)
  _format_odds                     moved from app.py:[Phase 1 line 1190]
  _correlation_impl_prob           moved from app.py:[Phase 1 line 2125]
  _american_to_prob                moved from app.py:[Phase 1 line 7670]
  _fmt_odds                        moved from app.py:[Phase 1 line 9015]

Percent / float (2)
  _fmt_pct                         moved from app.py:[Phase 1 line 9026]
  _to_float                        moved from app.py:[Phase 1 line 11550]

String processing (2)
  _strip_markdown_fences           moved from app.py:[Phase 1 line 1181]
  _ai_daily_counter_key            moved from app.py:[Phase 1 line 455]

Team / game normalization (4)
  _norm_team_name                  moved from app.py:[Phase 1 line 10441]
  _statsapi_norm_team              moved from app.py:[Phase 1 line 10864]
  _norm_team                       moved from app.py:[Phase 1 line 11542]
  _team_pair                       moved from app.py:[Phase 1 line 11546]

Schedule helpers (5)
  _espn_state_to_mlb_state         moved from app.py:[Phase 1 line 2643]
  _wnba_period_ordinal             moved from app.py:[Phase 1 line 2652]
  _schedule_is_postponed           moved from app.py:[Phase 1 line 2820]
  _schedule_priority               moved from app.py:[Phase 1 line 2825]
  _schedule_cache_key              moved from app.py:[Phase 1 line 2960]

Picker / game helpers (3)
  _team_key                        moved from app.py:[Phase 1 line 3040]
  _no_odds_predictions_cache_key   moved from app.py:[Phase 1 line 3564]
  _filter_stale_games              moved from app.py:[Phase 1 line 805]

Data conversion + finders (3)
  _py                              moved from app.py:[Phase 1 line 1527]
  _match_result_id                 moved from app.py:[Phase 1 line 7586]
  _find_analysis_row               moved from app.py:[Phase 1 line 7606]

Originally on the utils.py list, NOT moved (deps stay in app.py):
  _delete_file  — needs `_logger`; would create app↔utils circular import.
  _audit_log    — needs `_eprint` (uses `_redact` from src.redact + sys.stderr).
                  Could move if `_eprint` were moved too, but that expands scope.

Cross-module dependency:
  utils.py imports `_MLB_TEAM_NORM` and `_QUARTER_ORDINAL` from state.py
  (used by `_norm_team_name` and `_wnba_period_ordinal` respectively).
  Direction is utils -> state only; state has no knowledge of utils.

app.py changes:
  - 264 more lines removed (the 26 helper bodies + their leading
    comments, where present).
  - Added `from utils import *  # noqa: F401,F403` line immediately
    after the `from state import *` added in Phase 1.
  - Net: 12778 lines -> 12518 lines.

Verification (Phase 2):
  ✓ python3 -m py_compile utils.py
  ✓ python3 -m py_compile app.py
  ✓ `from utils import *` exposes all 26 helpers (via __all__).
  ✓ `from state import *; from utils import *` both succeed in one process.
  ✓ Spot-tests: _today_et(), _format_odds(150), _american_to_prob(-110)
    all return expected values.
  ✓ `import app` reaches the Flask check (i.e. clears all stdlib+numpy
    initialization) without any NameError.  Sandbox-only fatal is Flask
    not being installed -- production will succeed.

============================================================================
CUMULATIVE RESULT
============================================================================

app/app.py:        13,004 -> 12,518 lines  (-486, -3.7%)
app/state.py:      new, 360 lines
app/utils.py:      new, 351 lines

Items extracted:   43 state + 26 utils = 69 module-level definitions
Bare-name refs
 resolved by *:    468 in app.py

Reassignment audit (AST):
  All initial-declaration sites are now in state.py.
  Three post-initial rebinds (_STAT_LOOKUP_LOG_BUDGET x2, _sched x1)
  were left in app.py and documented above.

Branch: claude/split-utils-state

============================================================================
PR #277 — app/scheduler.py (3 items, narrow scope)
============================================================================

Branch: claude/split-scheduler
Base:   origin/main @ 468375f  (post PR #276 merge)
Source: app/app.py @ 12,518 lines

Pre-flight rule: STRICT move-only, no rewrites, __all__ required.

Original target (user request):
  "Extract APScheduler setup + ALL scheduled job functions"

What the dep audit found:
  9 scheduled job functions in app.py.  Each was analysed for free names
  referenced inside its body.  Only 2 could move under "no rewrites":

  Movable (clean):
    _run_meta_consensus_job        15 lines, deps: _eprint, traceback
    _run_personal_daily_limit_refresh 11 lines, deps: _eprint

  Plus _eprint itself (every job depends on it; only deps are sys + _redact
  from src.redact).

  Deferred (would require rewriting call signatures or moving large
  helper subtrees — both forbidden by the spec):
    _run_auto_analysis_job (171 lines)        — uses Flask app.test_client(),
                                                _log_model_picks, _noon_reconcile_*,
                                                _schedule_auto_retry,
                                                _write_auto_analysis_log
    _run_job1_final_settlement (29 lines)     — chains into _run_auto_settlement_job
    _run_job2_full_clear (130 lines)          — _supabase_cache_delete,
                                                _DAILY_PICKS_FILE et al.
    _run_job3_games_prefetch (42 lines)       — _fetch_raw_schedule
    _run_overnight_ai_gen (25 lines)          — hydrate_state
    _run_auto_settlement_job (384 lines)      — 13 app-only deps incl. Ledger,
                                                _grade_model_trackers, etc.
    _run_consolidated_refresh_cycle (133 lines) — 11 app-only deps incl.
                                                  _detect_*_changes,
                                                  _refresh_schedule_and_scores,
                                                  _rerun_single_game

  APScheduler setup block (app.py lines ~11784-11972):
    Hard deps on `app.debug`, `_werkzeug_main`, and `_logger` -- all of
    which the spec said keep in app.py.  Moving would require either a
    circular import or passing those in as args (a rewrite).  Block stays.

Pre-flight reassignment check (AST):
    Original FunctionDef sites for the 3 moved names: 3 (expected).
    Any post-initial reassignment: 0.
    Verdict: SAFE under `from scheduler import *`.

Functions moved (3):
    _eprint                            moved from app.py:613
    _run_meta_consensus_job            moved from app.py:9501
    _run_personal_daily_limit_refresh  moved from app.py:9691

Auxiliary import added to scheduler.py:
    from src.redact import redact as _redact   (preserves the name `_eprint`
                                                used at original site 623)

scheduler.py exports (__all__):
    _eprint, _run_meta_consensus_job, _run_personal_daily_limit_refresh

app.py changes:
    - 46 lines removed (the 3 functions + their preceding doc/comment
      blocks where present).
    - 1 new star-import line added right after `from utils import *`:
        `from scheduler import *  # noqa: F401,F403`
    - Net: 12,518 -> 12,478 lines (-40 net after counting the import).

Reference count after move (resolved via star-import):
    _eprint                              262 calls in app.py
    _run_meta_consensus_job                2 references
                                            (add_job site + scheduler.py def)
    _run_personal_daily_limit_refresh      1 reference (add_job site)

Verification:
    ✓ python3 -m py_compile passes on app.py and scheduler.py.
    ✓ `from scheduler import *` exposes all 3 moved names.
    ✓ `from state import *; from utils import *; from scheduler import *`
      all coexist in one process.
    ✓ Runtime smoke: _eprint('test') prints to stderr correctly.
    ✓ AST audit: zero dangling module-level redefs of moved names in app.py.
    ✓ `import app` clears stdlib + numpy + scheduler initialization; only
      fails on Flask (sandbox missing the dep, expected).

Cumulative across PRs #276 + #277:
    app/app.py:    13,004 -> 12,478 lines  (-526, -4.0%)
    app/state.py:    new, 360 lines
    app/utils.py:    new, 351 lines
    app/scheduler.py: new, 86 lines
    72 module-level definitions extracted from app.py.

============================================================================
PR #278a — hydrate_state cluster -> scheduler.py (2 commits)
============================================================================

Branch: claude/split-scheduler-278a
Base:   origin/main @ 0f33680  (post PR #277 merge)
Source: app/app.py @ 12,478 lines

Pre-flight rule: STRICT move-only, no rewrites, __all__ required.

Pre-flight reassignment check (AST):
    Move set:           6 names
    Original FunctionDef / Assign sites: 6 (all expected)
    Post-initial rebinds:                0
    Verdict: SAFE under `from <mod> import *`.

Free-name audit per moved function:
    _read_daily_snapshot   needs: json, sys, _DAILY_SNAPSHOT_FILE,
                                  _SNAPSHOT_ENABLED, _snapshot_lock
    _snapshot_is_today     needs: _SNAPSHOT_ENABLED, _today_et
    hydrate_state          needs: datetime, sys, _analysis_state,
                                  _filter_stale_games, _read_daily_snapshot,
                                  _snapshot_is_today, _today_et,
                                  _wnba_analysis_state
    _log_model_picks       needs: _analysis_state, _eprint,
                                  _wnba_analysis_state
                                  (plus inline src.* imports)

Commit 1 (b59278b)  state.py: appended 2 path constants
----------------------------------------------------------------------------

Names moved into state.py:
    _DAILY_SNAPSHOT_FILE   moved from app.py:288
    _DAILY_SNAPSHOT_TMP    moved from app.py:289

These should have been on PR #276's file-path-constants list -- the
audit there missed them.  Lifting now so the hydrate cluster moving
in Commit 2 doesn't have to reach back into app.py.  Both appended
to state.py's __all__.

app.py change: 12,478 -> 12,476 lines  (-2).

Commit 2 (this commit)  scheduler.py: appended 4-function cluster
----------------------------------------------------------------------------

Names moved into scheduler.py:
    _read_daily_snapshot   moved from app.py:722  (15 lines)
    _snapshot_is_today     moved from app.py:739  ( 8 lines)
    hydrate_state          moved from app.py:749  (68 lines)
    _log_model_picks       moved from app.py:10198 (21 lines)

scheduler.py header changes:
    + import json
    + from datetime import datetime
    + from state import *      (cluster needs _DAILY_SNAPSHOT_FILE,
                                _SNAPSHOT_ENABLED, _snapshot_lock,
                                _analysis_state, _wnba_analysis_state)
    + from utils import *      (cluster needs _today_et,
                                _filter_stale_games)
    __all__ expanded with the 4 new names.

Direction (no cycles):
    scheduler.py -> state.py, utils.py, src.*     (one-way down)
    app.py       -> scheduler.py, state.py, utils.py
    scheduler.py NEVER imports app.py.

app.py change: 12,476 -> 12,364 lines  (-112).
Total this PR:  12,478 -> 12,364 lines  (-114).

Reference count after move (resolved via star-import):
    _DAILY_SNAPSHOT_FILE, _DAILY_SNAPSHOT_TMP, _read_daily_snapshot,
    _snapshot_is_today, hydrate_state, _log_model_picks
    → 37 bare-name references in app.py, all via star-import.

Verification:
    ✓ py_compile clean on state.py, scheduler.py, app.py.
    ✓ `from scheduler import *` exposes all 7 names (PR #277's 3 +
      PR #278a's 4) via the updated __all__.
    ✓ `from state import *` exposes the 2 new path constants.
    ✓ Runtime: _read_daily_snapshot() reads the real snapshot file
      from sandbox correctly; _snapshot_is_today({}) returns False.
    ✓ AST audit: zero dangling module-level defs of moved names.
    ✓ `import app` reaches the Flask check (sandbox fatal as expected).

Cumulative across PRs #276 + #277 + #278a:
    app/app.py:       13,004 -> 12,364 lines  (-640, -4.9%)
    app/state.py:     360 -> 374 lines  (+14)
    app/utils.py:     351 lines  (unchanged)
    app/scheduler.py: 86 -> 221 lines  (+135)
    78 module-level definitions extracted from the original 13K-line monolith.

Unblocks (per the deferred list in PR #277):
    _run_overnight_ai_gen        was blocked on hydrate_state -- now free.
    _run_auto_settlement_job     was partially blocked on _log_model_picks
                                  (1 of its 13 deps).  Still blocked on the
                                  other 12; will need follow-up PRs.
    _run_auto_analysis_job       was partially blocked on _log_model_picks
                                  (1 of its 11 deps).  Still blocked on
                                  Flask app.test_client() + the other 9.

============================================================================
PR #279 — settlement bridge + overnight AI helpers -> scheduler.py
============================================================================

Branch: claude/split-scheduler-279
Base:   origin/main @ 24e3333  (post PR #278 merge)
Source: app/app.py @ 12,364 lines

Pre-flight rule: STRICT move-only.  EXCEPTION explicitly approved by user
this PR: a parallel `_logger = logging.getLogger("sports_betting")` line
is added to scheduler.py.  This is a NEW declaration (zero edits to any
existing code) and resolves to the same singleton as app.py's _logger
because Python's logging module is a process-wide name-keyed registry --
both `_logger` names point at the same object.  Verified at runtime:
`scheduler._logger is logging.getLogger("sports_betting")` is True.

Pre-flight reassignment check (AST):
    Move set:                             13 names
    Original FunctionDef/AnnAssign sites: 13 (all expected)
    Post-initial rebinds:                  0
    Verdict: SAFE under `from <mod> import *`.

User-named targets and their cascade closures:

  _run_overnight_ai_gen     (no cascade -- pure stdlib + reachable names)
  _persist_daily_budget     (no cascade)
  _grade_model_trackers     + cascade: _completed_games_from_scores
  _fetch_mlb_statsapi_scores + cascade: _statsapi_date_window,
                                        _statsapi_pick_et_date,
                                        _statsapi_schedule_index,
                                        _STATSAPI_BRIDGE_CACHE (-> state.py)
  _void_postponed_mlb_bets  + cascade: _fetch_mlb_linescore_raw,
                                        _debug_print,
                                        _today_et_str (also called from
                                        elsewhere in app.py; star-import
                                        resolves the legacy caller too)

Commit 1 (da45ea1)  state.py: added _STATSAPI_BRIDGE_CACHE
----------------------------------------------------------------------------
    _STATSAPI_BRIDGE_CACHE  moved from app.py:10239  (1 line)

This is mutable module-level dict state (keyed by ET date).  Belongs in
state.py with the other shared mutable state.  Lifted now so the
schedule-index helper moving in Commit 2 only has to star-import it.

app.py: 12,364 -> 12,363 lines  (-1).

Commit 2 (this commit)  scheduler.py: 12-function cluster
----------------------------------------------------------------------------

Functions moved (by original line number for readability):
    _debug_print                  moved from app.py:3217   (12 lines)
    _today_et_str                 moved from app.py:3231   ( 9 lines)
    _fetch_mlb_linescore_raw      moved from app.py:3242   (24 lines)
    _persist_daily_budget         moved from app.py:7150   (13 lines)
    _run_overnight_ai_gen         moved from app.py:9789   (25 lines)
    _void_postponed_mlb_bets      moved from app.py:9854   (75 lines)
    _completed_games_from_scores  moved from app.py:10031  (34 lines)
    _grade_model_trackers         moved from app.py:10067  (36 lines)
    _statsapi_pick_et_date        moved from app.py:10241  (13 lines)
    _statsapi_date_window         moved from app.py:10256  ( 9 lines)
    _statsapi_schedule_index      moved from app.py:10267  (39 lines)
    _fetch_mlb_statsapi_scores    moved from app.py:10308  (71 lines)

scheduler.py header changes:
    + import logging       (for the parallel _logger ref)
    + import time          (used by _statsapi_schedule_index for TTL math)
    + import urllib.request as _urlreq   (matches app.py:13 alias)
    + from datetime import timedelta, timezone
    + _logger = logging.getLogger("sports_betting")
                          (parallel ref to the same singleton, see above)
    __all__ expanded with the 12 new names.

Direction (no cycles):
    scheduler.py -> state.py / utils.py / src.*     (one-way down)
    app.py       -> scheduler.py / state.py / utils.py
    scheduler.py NEVER imports app.py.

app.py change: 12,363 -> 12,003 lines  (-360).
Total this PR:  12,364 -> 12,003 lines  (-361).

Reference count after move (resolved via star-import):
    12 moved function names + _STATSAPI_BRIDGE_CACHE
    → 12 bare-name references in app.py, all via star-import.

Verification:
    ✓ py_compile clean on state.py, scheduler.py, app.py.
    ✓ `from scheduler import *` exposes all 19 names (PR #277's 3 +
      PR #278a's 4 + PR #279's 12) via the updated __all__.
    ✓ `from state import *` exposes the new _STATSAPI_BRIDGE_CACHE.
    ✓ Runtime: _today_et_str() returns today's ET date; _statsapi_date_window()
      returns [date, date-1, date+1] as expected; _logger identity verified
      against logging.getLogger("sports_betting").
    ✓ AST audit: zero dangling module-level defs of moved names.
    ✓ `import app` clears stdlib + numpy + scheduler initialization; only
      fails on Flask (sandbox missing), production unaffected.

Cumulative across PRs #276 + #277 + #278a + #279:
    app/app.py:       13,004 -> 12,003 lines  (-1,001, -7.7%)
    app/state.py:     374 -> 378 lines  (+4)
    app/utils.py:     351 lines  (unchanged)
    app/scheduler.py: 221 -> 630 lines  (+409)
    91 module-level definitions extracted from the original 13K-line monolith.

Remaining blockers (from PR #277's deferred list):
    _run_auto_settlement_job (384 lines): had 13 app-only deps.  PR #279
      lifted 4 of them (_grade_model_trackers, _fetch_mlb_statsapi_scores,
      _void_postponed_mlb_bets, _persist_daily_budget) plus their helper
      subtrees.  Remaining ~9 deps still in app.py.
    _run_auto_analysis_job, _run_job2_full_clear, _run_job3_games_prefetch,
    _run_consolidated_refresh_cycle:
      still blocked on their own helper subtrees -- separate PRs.
    APScheduler bootstrap block (lines ~11400-11600):
      still blocked on `app.debug` + `_werkzeug_main`.

============================================================================
PR #280 — _run_auto_settlement_job + remaining helper cluster -> scheduler.py
============================================================================

Branch: claude/split-scheduler-280
Base:   origin/main @ 51e7f08  (post PR #279 merge)
Source: app/app.py @ 12,003 lines

Pre-flight rule: STRICT move-only.

Pre-flight reassignment check (AST):
    Move set:                                       8 names
    Original FunctionDef / Assign / AnnAssign:      8 (all expected)
    Post-initial rebinds in the move set:           3 (all for
                                                      _STAT_LOOKUP_LOG_BUDGET,
                                                      INSIDE the cluster --
                                                      see below)
    Verdict: SAFE -- the rebinder functions move TOGETHER with the
             variable to scheduler.py, so `global _STAT_LOOKUP_LOG_BUDGET`
             inside the moved functions resolves to scheduler.py's
             module scope.  Resolves the rebind blocker noted in
             PR #276's preamble.

Caller-graph audit confirmed the cluster is self-contained:
    _STAT_LOOKUP_LOG_BUDGET      callers: _stat_lookup_log,
                                          _reset_stat_lookup_log_budget
                                          (both move with it)
    _reset_stat_lookup_log_budget callers: _run_auto_settlement_job (moves)
    _stat_lookup_log              callers: _model_pick_stat_lookup (moves)
    _model_pick_stat_lookup       callers: _run_auto_settlement_job (moves)
    _final_scores_from            callers: _run_auto_settlement_job (moves)
    _SETTLE_GAMELOG_MEMO          callers: _model_pick_stat_lookup (moves)
    _auto_settlement_state        callers: _run_auto_settlement_job (moves)
                                          + get_auto_settlement_status (stays
                                            in app.py, resolves via
                                            `from state import *`)
    _run_auto_settlement_job      callers: _run_consolidated_refresh_cycle,
                                          _run_job1_final_settlement,
                                          admin_settle_now (route),
                                          bootstrap `_sched.add_job(...)`
                                          -- all stay in app.py and resolve
                                          the name via `from scheduler import *`.

Commit 1 (af9aa0e)  state.py: 2 settlement state dicts
----------------------------------------------------------------------------
    _auto_settlement_state  moved from app.py:1078  ( 7 lines, AnnAssign)
    _SETTLE_GAMELOG_MEMO    moved from app.py:9917  ( 5 lines including
                                                       its preceding doc
                                                       comment block,
                                                       AnnAssign)

Both appended to state.py's __all__.
app.py: 12,003 -> 11,991 lines (-12).

Commit 2 (this commit)  scheduler.py: settlement-job cluster
----------------------------------------------------------------------------

Names moved (sorted by original line for the # moved from comment):
    _STAT_LOOKUP_LOG_BUDGET        moved from app.py:9911   ( 1 line,
                                                              private --
                                                              NOT in __all__)
    _reset_stat_lookup_log_budget  moved from app.py:9914   ( 3 lines)
    _model_pick_stat_lookup        moved from app.py:9919   (67 lines)
    _stat_lookup_log               moved from app.py:9988   (10 lines)
    _final_scores_from             moved from app.py:10009  (21 lines)
    _run_auto_settlement_job       moved from app.py:10032  (384 lines)

scheduler.py header changes:
    NONE -- all imports needed (json, logging, sys, traceback, datetime,
            timedelta, timezone, _urlreq) were already added by PR #279,
            and the parallel _logger ref likewise.
    __all__ expanded with 5 new names (_STAT_LOOKUP_LOG_BUDGET intentionally
    excluded -- it's module-private state mutated only by the two
    functions also living in scheduler.py).

The _STAT_LOOKUP_LOG_BUDGET rebind decision:
    PR #276 documented this name as "kept in app.py" because moving it to
    state.py would have broken the `global ... = int(n)` rebinds inside
    _reset_stat_lookup_log_budget and `global ... -= 1` inside
    _stat_lookup_log -- under `from state import *`, the rebind would
    only have updated app.py's local name and state.py would have kept
    the original value.

    PR #280 resolves the issue by moving the variable AND both rebinder
    functions TOGETHER into scheduler.py.  Within a single module,
    `global X = value` rebinds the module attribute correctly, and
    Python's logging-style name resolution gives the same singleton
    behavior to any later importer.

    Runtime proof verified during this PR:
        scheduler._STAT_LOOKUP_LOG_BUDGET initially == 0
        _reset_stat_lookup_log_budget(5)
        scheduler._STAT_LOOKUP_LOG_BUDGET now == 5   ✓

Direction (no cycles):
    scheduler.py -> state.py / utils.py / src.*     (one-way down)
    app.py       -> scheduler.py / state.py / utils.py
    scheduler.py NEVER imports app.py.

app.py change: 11,991 -> 11,501 lines  (-490).
Total this PR:  12,003 -> 11,501 lines  (-502).

Reference count after move (resolved via star-import):
    The 8 moved names appear 5 times in app.py outside their original
    defs (route handler + bootstrap + cycle callers), all resolved via
    star-import.

Verification:
    ✓ py_compile clean on state.py, scheduler.py, app.py.
    ✓ `from scheduler import *` exposes 24 names (PR #277's 3 +
      PR #278a's 4 + PR #279's 12 + PR #280's 5) via __all__.
    ✓ `from state import *` exposes the 2 new state dicts.
    ✓ Runtime smoke (rebind proof): scheduler._STAT_LOOKUP_LOG_BUDGET
      rebinds correctly under _reset_stat_lookup_log_budget().
    ✓ _STAT_LOOKUP_LOG_BUDGET is NOT exposed via `from scheduler import *`
      (confirms it's module-private as designed).
    ✓ AST audit: zero dangling module-level defs in app.py.
    ✓ `import app` clears stdlib + numpy + scheduler; sandbox-only Flask
      fatal as expected.

Cumulative across PRs #276 + #277 + #278a + #279 + #280:
    app/app.py:       13,004 -> 11,501 lines  (-1,503, -11.6%)
    app/state.py:     378 -> 395 lines  (+17 cumulative)
    app/utils.py:     351 lines  (unchanged)
    app/scheduler.py: 630 -> 1,144 lines  (+514 cumulative)
    99 module-level definitions extracted from the original 13K-line monolith.

State of the #277 deferred list:
    ✅ _run_overnight_ai_gen     -- moved in PR #279.
    ✅ _run_auto_settlement_job  -- moved in THIS PR (#280).
    Still in app.py (separate PRs needed):
        _run_auto_analysis_job (171 lines) -- uses Flask app.test_client(),
                                              _log_model_picks (moved),
                                              _noon_reconcile_model_picks,
                                              _schedule_auto_retry,
                                              _write_auto_analysis_log
        _run_job1_final_settlement (29 lines) -- chains into the (now-moved)
                                                 _run_auto_settlement_job
        _run_job2_full_clear (130 lines)      -- 6 app-only deps
        _run_job3_games_prefetch (42 lines)   -- _fetch_raw_schedule
        _run_consolidated_refresh_cycle (133 lines) -- 11 app-only deps
        APScheduler bootstrap block            -- app.debug + _werkzeug_main

============================================================================
PR #281 — schedule helpers + 3 nightly jobs -> scheduler.py
============================================================================

Branch: claude/split-scheduler-281
Base:   origin/main @ 9db3067  (post PR #280 merge)
Source: app/app.py @ 11,501 lines

Pre-flight rule: STRICT move-only.

User target: _run_job1_final_settlement, _run_job2_full_clear,
             _run_job3_games_prefetch, _run_consolidated_refresh_cycle.

Dep audit on each:

  _run_job1_final_settlement   29 lines.  STAYS: only `traceback`
                                          (stdlib, already in scheduler.py).
                                          ✅ MOVES.
  _run_job2_full_clear        130 lines.  STAYS: _DAILY_PICKS_FILE,
                                          _supabase_cache_delete,
                                          traceback.  Cascades clean
                                          (path constant -> state.py,
                                          16-line helper -> scheduler.py).
                                          ✅ MOVES with 2 cascades.
  _run_job3_games_prefetch     42 lines.  STAYS: _fetch_raw_schedule,
                                          traceback.  Cascade clean
                                          (_fetch_raw_schedule pulls in
                                          _dedup_schedule_games and 3
                                          _normalize_* helpers; all
                                          either pure or only reference
                                          utils.py names).
                                          ✅ MOVES with 5 cascades.
  _run_consolidated_refresh_cycle 133 lines.  STAYS: _detect_game_changes,
                                          _detect_prop_changes,
                                          _refresh_cycle_state (state),
                                          _refresh_schedule_and_scores,
                                          _rerun_single_game.
                                          _rerun_single_game chains into
                                          _serialize (a large Flask-
                                          coupled function).
                                          ❌ DEFERRED.

Pre-flight reassignment check (AST):
    Move set:                                      10 names
    Original FunctionDef/Assign sites:             10 (all expected)
    Post-initial rebinds:                           0
    Verdict: SAFE under `from <mod> import *`.

Commit 1 (37fd982)  state.py: 1 path constant
----------------------------------------------------------------------------
    _DAILY_PICKS_FILE  moved from app.py:5403  (1 line)

Referenced by _run_job2_full_clear (moving) AND two Flask routes
(admin_model_reset, admin_reset_model_record, both staying); state.py
is the only place both sides can see it without a circular import.

app.py: 11,501 -> 11,500 lines (-1).

Commit 2 (this commit)  scheduler.py: 9 functions
----------------------------------------------------------------------------

Schedule-fetch helper subtree (6 functions):
    _supabase_cache_delete           moved from app.py:350    (16 lines)
    _normalize_espn_wnba_scoreboard  moved from app.py:2285   (64 lines)
    _dedup_schedule_games            moved from app.py:2413   (40 lines)
    _normalize_mlb_schedule          moved from app.py:2455   (43 lines)
    _normalize_wnba_schedule         moved from app.py:2500   (30 lines)
    _fetch_raw_schedule              moved from app.py:2534   (74 lines)

Nightly jobs (3 functions):
    _run_job1_final_settlement       moved from app.py:9516   ( 29 lines)
    _run_job2_full_clear             moved from app.py:9547   (130 lines)
    _run_job3_games_prefetch         moved from app.py:9679   ( 42 lines)

scheduler.py header changes:
    NONE -- all imports already in place (sys, traceback, json, datetime
            +timedelta+timezone, _urlreq, parallel _logger ref, plus the
            existing `from state import *` / `from utils import *`).
    __all__ expanded with the 9 new exported names.

External callers of moved names (resolve via star-import):
    _fetch_raw_schedule:
      - get_todays_schedule (Flask route)
      - schedule_for_date (Flask route)
      - _prefetch_no_odds_predictions (still in app.py)
      - _prefetch_schedules_next_n_days (still in app.py)
      - _refresh_schedule_and_scores (still in app.py)
    _normalize_espn_wnba_scoreboard:
      - wnba_schedule_proxy (Flask route)
    _DAILY_PICKS_FILE:
      - admin_model_reset (Flask route)
      - admin_reset_model_record (Flask route)
    Three nightly jobs:
      - APScheduler bootstrap block at line ~10773 (still in app.py).

Direction (no cycles):
    scheduler.py -> state.py / utils.py / src.*     (one-way down)
    app.py       -> scheduler.py / state.py / utils.py
    scheduler.py NEVER imports app.py.

app.py change: 11,500 -> 11,021 lines  (-479).
Total this PR:  11,501 -> 11,021 lines  (-480).

Verification:
    ✓ py_compile clean on state.py, scheduler.py, app.py.
    ✓ `from scheduler import *` exposes 33 names (PR #277's 3 +
      PR #278a's 4 + PR #279's 12 + PR #280's 5 + PR #281's 9).
    ✓ `from state import *` exposes _DAILY_PICKS_FILE.
    ✓ All 9 moved functions are callable from the importing module.
    ✓ AST audit: zero dangling module-level defs in app.py.
    ✓ 13 bare-name references in app.py resolve via star-import.
    ✓ `import app` clears stdlib + numpy + scheduler; sandbox-only
      Flask fatal as expected.

Cumulative across PRs #276 + #277 + #278a + #279 + #280 + #281:
    app/app.py:       13,004 -> 11,021 lines  (-1,983, -15.2%)
    app/state.py:     395 -> 399 lines  (+4)
    app/utils.py:     351 lines  (unchanged)
    app/scheduler.py: 1,144 -> 1,654 lines  (+510)
    109 module-level definitions extracted from the original 13K monolith.

#277 deferred list status:
    ✅ _run_overnight_ai_gen          -- moved in PR #279
    ✅ _run_auto_settlement_job       -- moved in PR #280
    ✅ _run_job1_final_settlement     -- moved in THIS PR
    ✅ _run_job2_full_clear           -- moved in THIS PR
    ✅ _run_job3_games_prefetch       -- moved in THIS PR
    Still in app.py (separate PRs):
        _run_auto_analysis_job (171 lines) -- Flask app.test_client() dep
        _run_consolidated_refresh_cycle (133 lines) -- _serialize chain
        APScheduler bootstrap block -- app.debug + _werkzeug_main

Remaining work after this PR:
    Two jobs and the bootstrap block remain in app.py.  Both jobs hit
    Big Flask-coupled subsystems (test_client + the serialization
    pipeline) -- they need significantly more architectural work, not
    just helper-extraction.

============================================================================
PR #282 — APScheduler bootstrap -> scheduler.init() [FIRST APPROVED REWRITE]
============================================================================

Branch: claude/split-scheduler-282
Base:   origin/main @ 6a9a250  (post PR #281 merge)
Source: app/app.py @ 11,021 lines

FIRST AND ONLY APPROVED REWRITE in the decomposition series.
============================================================
PRs #276 - #281 honored a strict "move-only, no rewrites" rule.  This
PR is the documented exception, explicitly approved by the user, and is
the only place the rule is broken.  The exception is scoped:

  1. The Werkzeug-double-import guard variable becomes a function
     parameter:
       Before (in app.py):
         _werkzeug_main = os.environ.get("WERKZEUG_RUN_MAIN", "false") == "true"
         if not _in_debug_mode or _werkzeug_main:
             ...192 lines of bootstrap...
       After:
         in scheduler.py:
             def init(app, werkzeug_main):
                 _in_debug_mode = app.debug
                 if not _in_debug_mode or werkzeug_main:
                     ...same 192 lines, verbatim...
                 return _sched
         in app.py:
             _werkzeug_main = os.environ.get("WERKZEUG_RUN_MAIN", "false") == "true"
             _sched = init(app, _werkzeug_main)

  2. app.debug is read from the passed-in `app` argument instead of the
     global `app` reference.  (Same name resolution, just made explicit.)

  3. A 3-line runtime import of `app` is added at the top of init():
         import app as _app_module
         _run_auto_analysis_job = _app_module._run_auto_analysis_job
         _run_consolidated_refresh_cycle = _app_module._run_consolidated_refresh_cycle
     This breaks the otherwise-circular dependency for the two jobs that
     still live in app.py (Flask-coupled, separately blocked).  Safe
     because init() is invoked from app.py's module-level code AFTER
     both function definitions, so app is already in sys.modules.  These
     three lines disappear when the two remaining jobs eventually move.

Everything else inside init() is the verbatim bootstrap block from
app.py:10284-10475 -- comments, prints, cron triggers, error handling,
manifest enumeration, all byte-for-byte the same.

Pre-flight reassignment check (AST):
    Move set:                                   3 names
        _NightlyRetrainStub  (class)
        nightly_retrain      (try-import + except-assign at app.py:239/244)
        init                 (new name introduced in scheduler.py)
    Original declarations:                      2 in app.py (stub, ntr)
    Post-initial rebinds:                       0 outside the try/except
                                                pair (which moves together
                                                and is the SAME definition).
    Verdict: SAFE.

Caller-graph audit:
    nightly_retrain         -- 4 callers in app.py:
                               admin_nightly_log (Flask route, stays),
                               admin_nightly_force (Flask route, stays),
                               admin_scheduler_health (Flask route, stays),
                               init() itself (moves with it).
                               The 3 staying routes resolve the name via
                               `from scheduler import *`.
    _NightlyRetrainStub     -- 1 caller in app.py:
                               admin_nightly_force (isinstance check).
                               Resolves via star-import.
    init                    -- 1 caller in app.py:
                               module-level bootstrap line (the new 2-line
                               replacement).
    _sched                  -- INITIAL declaration `_sched = None` stays
                               in app.py.  The reassignment inside the
                               bootstrap (`_sched = nightly_retrain.start()`)
                               now happens inside init() as a LOCAL var.
                               init() returns it; app.py captures with
                               `_sched = init(app, _werkzeug_main)`.
                               PR #276's "stays in app.py" decision for
                               _sched is preserved -- the rebind site is
                               still in app.py, just at one call site.

Items moved (app.py -> scheduler.py):
    _NightlyRetrainStub class        from app.py:228   (8 lines + 1 lead comment)
    nightly_retrain try/except       from app.py:238   (7 lines)
    Bootstrap block                  from app.py:10284 (192 lines)

scheduler.py changes:
    + def init(app, werkzeug_main) function (newly created -- the only
      new declaration introduced by this PR).
    + _NightlyRetrainStub class (moved verbatim).
    + nightly_retrain try/except (moved verbatim).
    + Added explanatory section header above the cluster.
    __all__ expanded with `_NightlyRetrainStub`, `nightly_retrain`, `init`.

app.py changes:
    - Stub class, try/except import, and the 192-line bootstrap block
      all removed.
    + 5-line replacement: a section header comment, the
      _werkzeug_main env read, and the init() call site.
    Net: 11,021 -> 10,819 lines  (-202 = -209 removed + 7 added).

Module direction (no cycles at module-load time):
    state.py / utils.py / src.*  - leaf modules
    scheduler.py imports state, utils, src.*, and at INIT-CALL-TIME imports
                                    app to fetch the 2 remaining job names
    app.py imports scheduler / state / utils as star-imports

    The lazy `import app` inside init() is safe because Python's import
    system already has app in sys.modules by the time init() is called
    (app.py's module-level code triggers init() AFTER all top-level
    definitions are executed).

Verification:
    ✓ py_compile clean on app.py and scheduler.py.
    ✓ `from scheduler import *` exposes 36 names (PR #277's 3 +
      PR #278a's 4 + PR #279's 12 + PR #280's 5 + PR #281's 9 +
      PR #282's 3) via updated __all__.
    ✓ Runtime checks:
        - nightly_retrain is the real src.nightly_retrain module
          (or the stub if the import fails -- same behavior as before).
        - _NightlyRetrainStub is a class.
        - inspect.signature(init) -> (app, werkzeug_main).  ✓ matches
          the user-specified contract.
    ✓ AST audit: zero dangling module-level defs of moved names in app.py.
    ✓ 6 bare-name refs in app.py resolve via star-import.
    ✓ `import app` reaches the Flask check (sandbox missing Flask, expected
      production-only check).  Importantly, the STARTUP "src.nightly_retrain
      OK" log line fires DURING the from-scheduler-import-* on app.py's
      side -- the timing of this print is now slightly earlier in the boot
      sequence (it runs as scheduler.py initializes rather than mid-app.py),
      but it still fires exactly once.  Operators should see no functional
      difference in Railway logs.

Cumulative across PRs #276 + #277 + #278a + #279 + #280 + #281 + #282:
    app/app.py:       13,004 -> 10,819 lines  (-2,185, -16.8%)
    app/state.py:     399 lines       (unchanged this PR)
    app/utils.py:     351 lines       (unchanged)
    app/scheduler.py: 1,654 -> 1,910 lines  (+256)
    111 module-level definitions + the bootstrap entry point now live
    outside app.py.

#277 deferred list status:
    ✅ APScheduler bootstrap            -- moved in THIS PR (refactored
                                           into init()).
    ✅ _run_overnight_ai_gen, _run_auto_settlement_job,
       _run_job1_final_settlement, _run_job2_full_clear,
       _run_job3_games_prefetch          -- moved across PRs #279-#281.
    Remaining in app.py (Flask-coupled, would each need their own
    architectural PR):
        _run_auto_analysis_job (171 lines) -- Flask app.test_client()
        _run_consolidated_refresh_cycle (133 lines) -- _serialize chain

Architectural note:
    Across PRs #276-#282 the codebase has gone from a single 13K-line
    file containing global state, pure helpers, scheduled jobs, the
    Flask app, and the bootstrap, to four files with clean dependency
    direction (state -> utils -> scheduler -> app).  Two jobs remain
    in app.py because they're irreducibly entangled with Flask
    request-cycle code (test_client) or the analysis serialization
    pipeline (_serialize).  Extracting either would require its own
    architectural decision (a fake test_client adapter, or a clean
    serialization-module extraction) -- both larger than any single
    refactor in this series and outside the move-only contract these
    PRs have honored.

============================================================================
PHASE 2 — PR #283  serializer.py extraction
============================================================================

Branch: claude/split-serializer-283
Base:   origin/main @ 6911aa2  (post PR #282 merge)
Source: app/app.py @ 10,819 lines

Pre-flight rule: STRICT move-only (Phase 2 returns to the strict rule;
the only approved rewrite was PR #282's init() refactor).

Phase 2 goal:  Extract the serialization layer (_serialize family) to
unblock _run_consolidated_refresh_cycle (currently the last unmoved
nightly job besides _run_auto_analysis_job, which is Flask-coupled).

Pre-flight audit (recorded on branch claude/audit-serializer-283):
  - _serialize           351 lines, ZERO Flask coupling
  - _serialize_wnba      161 lines, ZERO Flask coupling
  - _apply_correlation_rules 128 lines, ZERO Flask coupling
  - _format_rl_shap       17 lines, ZERO Flask coupling
  - Reassignment check:  0 post-initial rebinds across the 4-name set
  - Verdict: SAFE under `from serializer import *`.

Caller-graph audit:
  _serialize callers (6):
    schedule_for_date           Flask route   (resolves via star-import)
    analyze                     Flask route   (resolves via star-import)
    refresh_models              Flask route   (resolves via star-import)
    ai_breakdown                Flask route   (resolves via star-import)
    _build_chat_context         non-Flask     (resolves via star-import)
    _rerun_single_game          non-Flask     (UNBLOCK TARGET --
                                                moves in Commit 2 of this PR)
  _serialize_wnba callers (2):
    schedule_for_date           Flask route   (resolves via star-import)
    analyze_wnba                Flask route   (resolves via star-import)
  _apply_correlation_rules:  exclusively called by _serialize.
  _format_rl_shap:           exclusively called by _serialize.

Commit 1 (this commit)  serializer.py: 4 functions
----------------------------------------------------------------------------

Names moved (sorted by original line for the move comments):
    _serialize                 moved from app.py:1182  (351 lines)
    _serialize_wnba            moved from app.py:1537  (161 lines)
    _apply_correlation_rules   moved from app.py:1765  (128 lines)
    _format_rl_shap            moved from app.py:1895  ( 17 lines)

serializer.py header:
    from __future__ import annotations
    import logging
    import numpy as np
    from src.kelly import size_bet, confidence_tier_from_prob
    from state import *  # noqa: F401,F403
    from utils import *  # noqa: F401,F403
    _logger = logging.getLogger("sports_betting")    # parallel ref
                                                     # (same singleton as
                                                     # app.py + scheduler.py
                                                     # per Python's logging
                                                     # registry).
    __all__ = ["_serialize", "_serialize_wnba",
               "_apply_correlation_rules", "_format_rl_shap"]

The src.kelly import is wrapped in app.py's try/except + STARTUP-print
plumbing today; serializer.py just does the plain import.  An import
failure would surface as ImportError in both modules -- functionally
identical to today's SystemExit(1), but slightly less verbose in the
log.  Acceptable trade.

The parallel _logger reference follows the same pattern PR #279 set
for scheduler.py.  No new code is being introduced -- this is the
established "move-only" complement that lets the moved `_logger.warning(
...)` call sites in _serialize work verbatim.

Direction (no cycles):
    serializer.py -> state.py / utils.py / src.kelly  (one-way down)
    app.py        -> serializer.py                     (one-way down)
    scheduler.py  -> serializer.py                     (one-way down)
    serializer.py NEVER imports app.py or scheduler.py.

app.py changes:
    - 659 lines removed (the 4 functions + their preceding comment blocks).
    - 1 new star-import line added right after `from scheduler import *`:
        from serializer import *  # noqa: F401,F403
    - Net: 10,819 -> 10,165 lines  (-654).

Reference count after move (resolved via star-import):
    10 bare-name references in app.py to the 4 moved names.

Verification:
    ✓ py_compile clean on app.py and serializer.py.
    ✓ `from serializer import *` exposes all 4 names via __all__.
    ✓ All 4 are callable from importing modules.
    ✓ serializer._logger is identity-equal to
      logging.getLogger("sports_betting") -- confirms the parallel
      reference is the same singleton.
    ✓ AST audit: zero dangling module-level defs of moved names.
    ✓ `import app` clears stdlib + numpy + scheduler + serializer
      initialization; sandbox-only Flask fatal expected.

----------------------------------------------------------------------------
Note: Commit 2 (_rerun_single_game move) DEFERRED to PR #284.
----------------------------------------------------------------------------

The original Phase 2 plan for PR #283 included a Commit 2 that would move
_rerun_single_game to scheduler.py.  The Phase 2 audit summary (branch
claude/audit-serializer-283) reported that _rerun_single_game's only
blocker was _serialize -- so once Commit 1 above moved _serialize to
serializer.py, _rerun_single_game would be unblocked.

Re-auditing _rerun_single_game on this branch after Commit 1 landed
revealed that the original audit summary was incomplete.  The function
has three additional app.py-local stay-deps that the summary missed:

    _update_result_in_state    (10 lines, no STAYS of its own -- CLEAN)
    _update_snapshot_game      (17 lines, needs _write_daily_snapshot ->
                                            _supabase_cache_set chain)
    _ensure_no_odds_predictor  (120 lines, needs _no_odds_predictor +
                                            _no_odds_predictor_failed
                                            state dicts)

Full cascade needed to move _rerun_single_game cleanly:
    _update_result_in_state        ( 10 lines)
    _update_snapshot_game          ( 17 lines)
    _ensure_no_odds_predictor      (120 lines)
    _write_daily_snapshot          ( 57 lines)
    _supabase_cache_set            ( 20 lines)
    _no_odds_predictor             (  1 line state dict)
    _no_odds_predictor_failed      (  1 line state dict)
    _rerun_single_game itself      ( 73 lines)
    -----------------------------------
    Total:                          ~299 lines, 6 functions + 2 state dicts

None of the cascade members have Flask coupling.  Cascade is tractable
but exceeds the narrow scope the user approved for this PR.  Deferred
to PR #284 so the audit correction can be reviewed alongside the
expanded move plan, rather than landing a 300-line cascade hidden under
the original "single function" commit description.

PR #283 ships with Commit 1 only.


============================================================================
PR #284 — _rerun_single_game full cascade
============================================================================

Branch: claude/split-scheduler-284
Base:   origin/main @ 5ae2876  (post PR #283 merge)
Source: app/app.py @ 10,165 lines

Pre-flight rule: STRICT move-only.

This PR executes the cascade plan deferred from PR #283 (see deferral
note at the end of the PR #283 section above).

Pre-flight reassignment check (AST):
    Move set:                                       8 names
    Original FunctionDef / AnnAssign sites:         8 (all expected)
    Post-initial rebinds:                            0
    Verdict: SAFE under `from <mod> import *`.

Commit 1 (9a3aca6)  state.py: 2 no-odds-predictor state dicts
----------------------------------------------------------------------------
    _no_odds_predictor          moved from app.py:1737  (1 line, AnnAssign)
    _no_odds_predictor_failed   moved from app.py:1739  (1 line, AnnAssign)

Both referenced by 3 app.py functions:
    _ensure_no_odds_predictor   (moves in commit 2)
    _boot_predictions_warmup    (stays in app.py)
    _run                        (route helper, stays in app.py)

The two staying callers reach the same dict via star-import, so
mutations land on the shared object.

app.py: 10,165 -> 10,153 lines  (-12).

Commit 2 (this commit)  scheduler.py: 6 functions
----------------------------------------------------------------------------

Names moved (sorted by original line):
    _supabase_cache_set         moved from app.py:317   ( 20 lines)
    _write_daily_snapshot       moved from app.py:699   ( 57 lines)
    _ensure_no_odds_predictor   moved from app.py:1742  (120 lines)
    _update_result_in_state     moved from app.py:9045  ( 10 lines)
    _update_snapshot_game       moved from app.py:9057  ( 17 lines)
    _rerun_single_game          moved from app.py:9076  ( 73 lines)

scheduler.py header:
    NONE -- all imports needed (logging, sys, traceback, json, datetime
            +timedelta+timezone, _urlreq, parallel _logger ref, plus the
            existing `from state import *` / `from utils import *` /
            `from serializer import *`) were already in place.
    __all__ expanded with the 6 new exported names.

External callers in app.py (resolve via star-import):
    _supabase_cache_set         4 callers stay in app.py:
                                  _write_analysis_timestamp,
                                  _clear_snapshot_sport,
                                  _save_wnba_analysis_cache,
                                  _save_analysis_cache.
    _write_daily_snapshot       2 callers stay in app.py:
                                  analyze (Flask route),
                                  analyze_wnba (Flask route).
    _ensure_no_odds_predictor   1 caller stays:
                                  _predict_no_odds_game.
    _rerun_single_game          1 caller stays:
                                  _run_consolidated_refresh_cycle (which
                                  this PR was meant to fully unblock,
                                  but see note below).

Direction (no cycles):
    scheduler.py -> state.py / utils.py / serializer.py / src.*
    app.py       -> scheduler.py / state.py / utils.py / serializer.py
    scheduler.py NEVER imports app.py.

app.py change: 10,153 -> 9,856 lines  (-297).
Total this PR:  10,165 -> 9,856 lines  (-309).

Verification:
    ✓ py_compile clean on state.py, scheduler.py, app.py.
    ✓ `from scheduler import *` exposes 42 names total via __all__.
    ✓ `from state import *` exposes the 2 new dicts.
    ✓ All 6 moved functions callable from importing modules.
    ✓ AST audit: zero dangling module-level defs of moved names.
    ✓ 15 bare-name refs in app.py resolve via star-import.
    ✓ `import app` reaches the Flask check (sandbox fatal as expected).

_run_consolidated_refresh_cycle status (PARTIAL UNBLOCK)
----------------------------------------------------------------------------

Re-audit after Commit 2 lands:
    Reachable via star-import (7):
        _analysis_state, _eprint, _match_result_id, _refresh_cycle_lock,
        _rerun_single_game (NEW), _run_auto_settlement_job, _wnba_analysis_state
    STAYS in app.py (4 real blockers + 5 stdlib already in scheduler.py):
        _detect_game_changes        -- function, not yet moved
        _detect_prop_changes        -- function, not yet moved
        _refresh_cycle_state        -- module-level state dict, not yet moved
        _refresh_schedule_and_scores -- function, not yet moved
        (datetime, sys, time, timedelta, timezone all already in scheduler.py)

So _rerun_single_game is now fully extracted, but
_run_consolidated_refresh_cycle is still blocked on 4 additional names.
PR #285 should cascade those (plus their own helpers, if any) to land
the last non-Flask job from the #277 deferred list.

The "fully unblocked" assertion in this PR's directive was based on the
PR #281 dep audit that listed only _rerun_single_game as the chain
blocker.  That audit treated _detect_*_changes,
_refresh_schedule_and_scores, and _refresh_cycle_state as separate
STAYS but the deferral plan didn't carry them forward into PR #284's
scope.

Cumulative across PRs #276 + #277 + #278a + #279 + #280 + #281 + #282
                  + #283 + #284:
    app/app.py:       13,004 -> 9,856 lines   (-3,148, -24.2%)
    app/state.py:     399 -> 416 lines  (+17)
    app/utils.py:     351 lines  (unchanged)
    app/scheduler.py: 1,910 -> 2,231 lines  (+321)
    app/serializer.py: 719 lines  (unchanged from PR #283)
    123 module-level definitions extracted from the original 13K-line
    monolith.

#277 deferred list final status:
    ✅ APScheduler bootstrap              -- moved in PR #282
    ✅ _run_overnight_ai_gen              -- moved in PR #279
    ✅ _run_auto_settlement_job           -- moved in PR #280
    ✅ _run_job1_final_settlement         -- moved in PR #281
    ✅ _run_job2_full_clear               -- moved in PR #281
    ✅ _run_job3_games_prefetch           -- moved in PR #281
    ⚠️ _run_consolidated_refresh_cycle    -- ~50% unblocked: cascade
                                              done up to _rerun_single_game.
                                              4 remaining deps for PR #285.
    ❌ _run_auto_analysis_job             -- Flask app.test_client() dep,
                                              architectural decision needed.
